# These files are GENERATED automatically by build.sh (via python_app/src/gen_keys.py).
# Do NOT create manually. After running build.sh, they will appear here as PEM content:
# -----BEGIN PRIVATE KEY-----
# [ECDSA private key bytes in base64]
# -----END PRIVATE KEY-----
# -----BEGIN PUBLIC KEY-----
# [ECDSA public key bytes in base64]
# -----END PUBLIC KEY-----
# If manual: Run cd python_app/src && python gen_keys.py && mv *.pem ../../keys/
# Gitignore protects them – never commit real keys!